#include<stdio.h>
// std = standard (padrão)
// io = input output (entrada e saída)
//.h = header
main(int q, char *txt[])
{
	printf("Argumentos externos");
	printf("\n qtd= %d",q);
	printf("\n txt[0] = %s", txt[0]);
}
